{
  "name": "miniauth-frontend-retired",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The production MiniAuth UI is plain HTML/CSS/JS embedded from ../MiniAuth.IdentityAuth/wwwroot.",
  "scripts": {
    "build": "node -e \"console.log('MiniAuth UI is embedded from ../MiniAuth.IdentityAuth/wwwroot; no frontend build is required.')\""
  },
  "dependencies": {},
  "devDependencies": {}
}
